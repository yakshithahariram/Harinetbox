---
  - hosts: all
    gather_facts: False
        
    connection: network_cli
  
    vars:
     # ansible_user: admin
    #  ansible_ssh_pass: cisco
      ansible_network_os: ios
    
    tasks:
    # Collect all facts from the device
    - name: IOS FACTS
      ios_facts:
      os_port_facts:
       # gather_subset:
        #  - "hardware"
        
    - name: DISPLAY IOS HOST & PORT NUMBER
      debug:
        msg: "The port for {{ ansible_net_hostname }} is : {{ netbox.netbox.netbox_console_port }}"
