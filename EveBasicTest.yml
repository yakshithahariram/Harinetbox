---
  - hosts: all
    gather_facts: False
        
    connection: network_cli
  
    vars:
     # ansible_user: admin
    #  ansible_ssh_pass: cisco
      ansible_network_os: ios
    
    tasks:
    # Collect all facts from the device
    - name: IOS FACTS
      
      ios_facts:
       # gather_subset:
        #  - "hardware"
 
    - name: DISPLAY IOS HOSTNAME
      debug:
        msg: "The hostname is: {{ ansible_net_config }}: {{ ansible_net_gather_subset }}"
