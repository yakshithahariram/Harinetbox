---
  - name: CISCO devices configurations
    hosts: all
    gather_facts: False
    connection: local
    vars:
      ios_provider:
        username: cisco
        password: cisco
      ansible_command_timeout: 60
      
    tasks:
      - name: AAA config
        src: "AAA.j2"
        provider: "{{ ios_provider }}"
